// Codes by mahdi taha
// Importing type of configs of next.js
/** @type {import('next').NextConfig} */

// Defining the configs
const nextConfig = {};

// Exporting the configs as default
export default nextConfig;
