// Codes by mahdi tasha
// Importing type of next.js config
import type { NextConfig } from 'next';

// Defining the configs
const nextConfig: NextConfig = {};

// Exporting the configs as default
export default nextConfig;
